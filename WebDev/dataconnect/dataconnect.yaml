specVersion: "v1"
serviceId: "webdev"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "webdev-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
